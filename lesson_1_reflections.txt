How did viewing a diff between two versions of a file help you see the bug that was introduced?
	diff shows you the lines that has changed. That's, shows you the difference between 2 files. So, seeing what has changed, you can found the error.

How could having easy access to the entire history of a file make you a more efficient programmer in the long term?
	Having differents versions of a file, makes more easy to go back to a previous version, or to fix problems in the new versions.

What do you think are the pros and cons of manually choosing when to create a commit, like you do in Git, vs having versions automatically saved, like Google Docs does?
	Pros:	You can chose to save only when there is an important change
			You can comment the changes you made.

	Cons:	You can forget to save it

Why do you think some version control systems, like Git, allow saving multiple files in one commit, while others, like Google Docs, treat each file separately?
	Because they were made for different purposes. In google Docs, different files are not related, but in programing languages, every file of a same project is related.

How can you use the commands git log and git diff to view the history of files?
	using git log I can see the changes saved over time and a short description of that change, identified with an ID.
	using git diff over two ID's, I can see the changes that where made.

How might using version control make you more confident to make changes that could break something?
	If you break something, you always can go back to a previous state
